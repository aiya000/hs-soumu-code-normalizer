name:                soumu-code-normalizer
version:             0.1.0.0
github:              "aiya000/soumu-code-normalizer"
license:             BSD3
author:              "aiya000"
maintainer:          "aiya000.develop@gmail.com"
copyright:           "aiya000"

extra-source-files:
- README.md
- ChangeLog.md

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Simple

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on GitHub at <https://github.com/aiya000/soumu-code-normalizer#readme>

dependencies:
- base >= 4.7 && < 5

library:
  source-dirs: src

executables:
  soumu-code-normalizer-exe:
    main:                Main.hs
    source-dirs:         app
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - soumu-code-normalizer

tests:
  soumu-code-normalizer-test:
    main:                Spec.hs
    source-dirs:         test
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - soumu-code-normalizer
